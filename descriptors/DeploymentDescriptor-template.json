{
  "srvcId": "${artifactId}-${version}",
  "nodeId": "localhost",
  "descriptor": {
    "exec": "java -Dport=%p -jar ../mod-source-record-storage/mod-source-record-storage-fat.jar -Dhttp.port=%p embed_postgres=true"
  }
}
