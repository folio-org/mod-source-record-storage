{
  "id": "${artifactId}-${version}",
  "name": "Data Import Storage Module",
  "provides": [
    {
      "id": "${artifactId}",
      "version": "1.0",
      "handlers": [
        {
          "methods": ["GET"],
          "pathPattern": "/data-storage/items/{itemId}"
        },
        {
          "methods": ["DELETE"],
          "pathPattern": "/data-storage/items/{itemId}"
        }
      ]
    },
    {
      "id": "_tenant",
      "version": "1.0",
      "interfaceType": "system",
      "handlers": [
        {
          "methods": ["POST"],
          "pathPattern": "/_/tenant"
        }
      ]
    }
  ],
  "requires": [],
  "launchDescriptor": {
    "dockerImage": "${artifactId}:${version}",
    "dockerArgs": {
      "HostConfig": {"PortBindings": {"8081/tcp": [{"HostPort": "%p" }] } }
    },
    "dockerPull": false
  }
}
