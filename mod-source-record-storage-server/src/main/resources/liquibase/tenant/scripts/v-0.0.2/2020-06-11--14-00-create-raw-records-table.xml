<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.9.xsd">

  <changeSet id="2020-06-11--14-00-create-raw-records-table" author="WilliamWelling">
    <createTable tableName="raw_records_lb">
      <column name="id" type="uuid">
        <constraints primaryKey="true" nullable="false"/>
      </column>
      <column name="content" type="text">
        <constraints nullable="false"/>
      </column>
    </createTable>
  </changeSet>

  <changeSet id="2020-06-11--14-01-add-raw-records-records-foreign-key" author="WilliamWelling">
    <addForeignKeyConstraint
        baseColumnNames="id"
        baseTableName="raw_records_lb"
        baseTableSchemaName="${database.defaultSchemaName}"
        constraintName="fk_raw_records_records"
        deferrable="true"
        initiallyDeferred="true"
        onDelete="CASCADE"
        onUpdate="RESTRICT"
        referencedColumnNames="id"
        referencedTableName="records_lb"
        referencedTableSchemaName="${database.defaultSchemaName}"/>
  </changeSet>

  <changeSet id="2020-06-11--14-02-create-update-records-set-has-raw-record-function" author="WilliamWelling">
    <createProcedure>
      create function ${database.defaultSchemaName}.update_records_set_has_raw_record() returns trigger
          language plpgsql
      as
      $$
      BEGIN
          IF (TG_OP = 'DELETE') THEN
            UPDATE ${database.defaultSchemaName}.records_lb SET has_raw_record = false WHERE id = OLD.id;
            RETURN OLD;
          ELSE
            UPDATE ${database.defaultSchemaName}.records_lb SET has_raw_record = true WHERE id = NEW.id;
            RETURN NEW;
          END IF;
      END;
      $$;
    </createProcedure>
  </changeSet>

  <changeSet id="2020-06-11--14-03-create-update-records-set-updated-date-trigger" author="WilliamWelling">
    <sql>
      create trigger update_records_set_has_raw_record
          after insert or update or delete
          on ${database.defaultSchemaName}.raw_records_lb
          for each row
      execute procedure ${database.defaultSchemaName}.update_records_set_has_raw_record();
    </sql>
  </changeSet>

</databaseChangeLog>
